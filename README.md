# Learn Maven

A simple Java project for learning Maven basics.

This project demonstrates Maven project structure, dependency management (JUnit), and build plugins.
It includes a HelloWorld application and a Greetings class with unit tests.
